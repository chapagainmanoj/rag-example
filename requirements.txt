# Core
pypdf>=4.0.0
chromadb>=0.4.0
python-dotenv>=1.0.0

# OpenAI (skip if using Ollama only)
openai>=1.0.0


# Anthropic + Voyage embeddings (skip if using OpenAI/Ollama only)
anthropic>=0.25.0
voyageai>=0.2.0


# Pinecone (optional)
pinecone-client>=3.0.0